# SPDX-License-Identifier: CC0-1.0
# Tuning file for OmniVision OV02E10 on Intel IPU7 (Lunar Lake)
# Used by libcamera's Simple pipeline handler / Software ISP
#
# Symmetric strong saturation boost (tune-ccm.sh preset #5).
# Green tint under harsh lighting is exposure-related, not a CCM issue.
# Use tune-ccm.sh to interactively find the best color preset.
#
# NOTE: libcamera 0.5.x uses Lut; 0.6+ uses Adjust (replaces Lut).
# The installer handles this automatically, but if editing manually:
#   v0.5.x: keep Lut, remove Adjust
#   v0.6+:  keep Adjust, remove Lut
%YAML 1.1
---
version: 1
algorithms:
  - BlackLevel:
  - Awb:
  - Ccm:
      ccms:
        - ct: 5000
          ccm: [ 1.4, -0.2, -0.2,
                -0.2,  1.4, -0.2,
                -0.2, -0.2,  1.4 ]
  - Lut:
  - Adjust:
  - Agc:
...
